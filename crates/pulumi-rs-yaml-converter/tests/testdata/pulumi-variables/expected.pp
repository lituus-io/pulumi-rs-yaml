output currentCwd {
	__logicalName = "currentCwd"
	value = cwd()
}

output currentProject {
	__logicalName = "currentProject"
	value = project()
}

output currentStack {
	__logicalName = "currentStack"
	value = stack()
}
